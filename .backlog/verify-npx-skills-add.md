# Verify npx skills add Compatibility

Ensure the repo works with `npx skills add https://github.com/mdziedzi/agent-backlog --skill agent-backlog`.

## Tasks
- [ ] Research what directory structure / metadata `npx skills add` expects
- [ ] Check if a `package.json` or `skills.json` manifest is required
- [ ] Adjust repo structure if needed
- [ ] Test installation end-to-end
