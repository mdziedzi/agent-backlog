# Document Phase Purpose: One Run Per Phase

Clarify in the skill that phases exist so a coding agent can complete one phase per run. Phases should be sized and scoped to fit a single agent session.

- [ ] Update `SKILL.md` Phase definition to state the purpose: one agent run completes one phase
- [ ] Add guidance on sizing phases so they fit a single session
- [ ] Mention this in the "Workflow" section where relevant
